# Stage 10A — staging: API storage + Auth API + Asset upload
# Loaded by `npm run dev:staging` / Playwright staging (port 8082).

STORAGE_PROVIDER=api
CMS_STORAGE_PROVIDER=api
VITE_STORAGE_PROVIDER=api
VITE_CMS_STORAGE_PROVIDER=api
