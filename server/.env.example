# Copied by build:hostinger when server/.env is missing.
# On EasyPanel, prefer setting these in the panel UI (do not bake secrets into the image).
DATABASE_URL="file:./prisma/cms.db"
# PORT is injected by EasyPanel — do not hardcode in production.
# CMS_API_CORS_ORIGIN=https://seu-dominio.com
