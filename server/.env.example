# Copied automatically by build:hostinger when server/.env is missing.
DATABASE_URL="file:./prisma/cms.db"
PORT=3001
# Optional: leave unset to reflect request Origin (recommended for Hostinger Node).
# CMS_API_CORS_ORIGIN=https://seu-dominio.com
